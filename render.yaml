services:
  - type: web
    name: school-bus-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false
      - key: JWT
        sync: false
      - key: FRONTEND_URL
        sync: false
